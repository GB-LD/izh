npx lint-staged

# Run tests related to staged files
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACMR -- '*.ts' '*.tsx')
if [ -n "$STAGED_FILES" ]; then
  npx vitest related --run $STAGED_FILES
fi
